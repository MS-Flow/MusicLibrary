<Window x:Class="MusicLibrary.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MusicLibrary"
        xmlns:model="clr-namespace:MusicLibrary.Models"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="2*" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <!-- skapa playlist -->
                <RowDefinition Height="Auto"/>
                <!-- välj playlist -->
                <RowDefinition Height="Auto"/>
                <!-- ändra namn -->
                <RowDefinition Height="*"/>
                <!-- TreeView -->
                <RowDefinition Height="Auto"/>
                <!-- lägg till-knapp -->
            </Grid.RowDefinitions>

            <StackPanel 
            Grid.Row="0"
            Orientation="Horizontal"
            Margin="5"
            Height="20">

                <TextBox Width="150"
                 Margin="0,0,5,0"
                 Text="{Binding NewPlaylistName, UpdateSourceTrigger=PropertyChanged}" />

                <Button 
                    Content="Create Playlist"
                    Width="95"
                    Margin="2.5 0 0 0"
                    Command="{Binding CreatePlaylistCommand}"
                />
            </StackPanel>

            <Label 
                Content="Playlists:" 
                Grid.Row="1"
                Grid.Column="0" Margin="0,5,0,0"/>

            <ComboBox 
              Grid.Row="2"
              Grid.Column="0"
              ItemsSource="{Binding Playlists}"
              SelectedItem="{Binding SelectedPlaylist, Mode=TwoWay}"
              DisplayMemberPath="Name"
              Margin="5 0, 0, 5"/>

            <StackPanel Grid.Row="2" 
                    Orientation="Horizontal"
            Margin="5">

            
            </StackPanel>




            <TreeView x:Name="myTreeView" Grid.Row="4" Grid.Column="0" SelectedItemChanged="TreeView_SelectedItemChanged">
                <TreeView.Resources>
                    <HierarchicalDataTemplate DataType="{x:Type model:Artist}" ItemsSource="{Binding Albums}">
                        <TextBlock>
                        <Run Text="{Binding Name}" />
                        <Run Text="(" /><Run Text="{Binding Albums.Count, Mode=OneWay}" />
                        <Run Text="albums)" />
                        </TextBlock>
                    </HierarchicalDataTemplate>
                    <HierarchicalDataTemplate DataType="{x:Type model:Album}" ItemsSource="{Binding Tracks}">
                        <TextBlock Text="{Binding Title}" />
                    </HierarchicalDataTemplate>
                    <HierarchicalDataTemplate DataType="{x:Type model:Track}">
                        <TextBlock Text="{Binding Name}" />
                    </HierarchicalDataTemplate>
                </TreeView.Resources>
            </TreeView>

            <Button 
            Grid.Row="3"
              Content="Add Track To Playlist"
              Margin="5"
              Command="{Binding AddTrackToPlaylistCommand}" />

        </Grid>
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <!-- loading text -->
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="500"/>

                <!-- DataGrid -->
            </Grid.RowDefinitions>
            <Label Content="{Binding SelectedPlaylist, Mode=TwoWay}"></Label>
            <Button Content="Save Playlist Name"
                Margin="2.5,0,0,0"
                HorizontalAlignment="Right"
                Command="{Binding UpdatePlaylistCommand}" Width="95" 
            />
            <!--Text="{Binding EditPlaylistName, UpdateSourceTrigger=PropertyChanged}" TextChanged="TextBox_TextChanged" />-->


            <DataGrid x:Name="myDataGrid" 
                  Grid.Row="1"
                  AutoGenerateColumns="False"
                  IsReadOnly="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserResizeColumns="False"
                  CanUserResizeRows="False"
                  CanUserReorderColumns="False"
                  CanUserSortColumns="True"
                  SelectionMode="Extended"
                  SelectionUnit="CellOrRowHeader"
                  RowHeaderWidth="20"
                  HorizontalScrollBarVisibility="Auto"
                  VerticalScrollBarVisibility="Auto"
                  AlternationCount="2"
                  RowBackground="White"
                  AlternatingRowBackground="NavajoWhite"
                  ItemsSource="{Binding Tracks}"
                  ScrollViewer.ScrollChanged="DataGrid_ScrollChanged"
                  IsEnabled="{Binding IsLoading,
                              Converter={StaticResource InverseBoolConverter}}"
                  SelectedItem="{Binding SelectedPlaylistTrack, Mode=TwoWay}"
                      
                      
        >
                <DataGrid.RowHeaderStyle>
                    <Style TargetType="DataGridRowHeader">
                        <Setter Property="Content" Value="🗑"/>
                        <Setter Property="ToolTip" Value="Ta bort låt från playlist"/>
                        <Setter Property="HorizontalContentAlignment" Value="Center"/>
                        <Setter Property="Cursor" Value="Hand"/>
                        <Setter Property="Foreground" Value="DarkRed"/>
                        <Setter Property="Padding" Value="5"/>
                        <EventSetter Event="PreviewMouseLeftButtonDown"
                     Handler="RowHeader_DeleteClicked"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="Red"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowHeaderStyle>



                <DataGrid.Columns>
                    <DataGridTextColumn Header="Track"
                            Binding="{Binding Name}" FontStyle="Italic"/>

                    <DataGridTextColumn Header="Album"
                            Binding="{Binding Album.Title}" />

                    <DataGridTextColumn Header="Length (ms)"
                            Binding="{Binding Milliseconds}" />
                </DataGrid.Columns>
            </DataGrid>


        </Grid>
    </Grid>
</Window>